create or replace pipe DimAccount_pipe as copy into DimAccount from @my_stage/DimAccount/ file_format = my_format;
create or replace pipe DimCurrency_pipe as copy into DimCurrency from @my_stage/DimCurrency/ file_format = my_format;
create or replace pipe DimCustomer_pipe as copy into DimCustomer from @my_stage/DimCustomer/ file_format = my_format;
create or replace pipe DimDate_pipe as copy into DimDate from @my_stage/DimDate/ file_format = my_format;
create or replace pipe DimDepartmentGroup_pipe as copy into DimDepartmentGroup from @my_stage/DimDepartmentGroup/ file_format = my_format;
create or replace pipe DimEmployee_pipe as copy into DimEmployee from @my_stage/DimEmployee/ file_format = my_format;
create or replace pipe DimGeography_pipe as copy into DimGeography from @my_stage/DimGeography/ file_format = my_format;
create or replace pipe DimOrganization_pipe as copy into DimOrganization from @my_stage/DimOrganization/ file_format = my_format;
create or replace pipe DimProduct_pipe as copy into DimProduct from @my_stage/DimProduct/ file_format = my_format;
create or replace pipe DimProductCategory_pipe as copy into DimProductCategory from @my_stage/DimProductCategory/ file_format = my_format;
create or replace pipe DimProductSubcategory_pipe as copy into DimProductSubcategory from @my_stage/DimProductSubcategory/ file_format = my_format;
create or replace pipe DimPromotion_pipe as copy into DimPromotion from @my_stage/DimPromotion/ file_format = my_format;
create or replace pipe DimReseller_pipe as copy into DimReseller from @my_stage/DimReseller/ file_format = my_format;
create or replace pipe DimSalesReason_pipe as copy into DimSalesReason from @my_stage/DimSalesReason/ file_format = my_format;
create or replace pipe DimSalesTerritory_pipe as copy into DimSalesTerritory from @my_stage/DimSalesTerritory/ file_format = my_format;
create or replace pipe DimScenario_pipe as copy into DimScenario from @my_stage/DimScenario/ file_format = my_format;
create or replace pipe FactAdditionalInternationalProductDescription_pipe as copy into FactAdditionalInternationalProductDescription from @my_stage/FactAdditionalInternationalProductDescription/ file_format = my_format;
create or replace pipe FactCallCenter_pipe as copy into FactCallCenter from @my_stage/FactCallCenter/ file_format = my_format;
create or replace pipe FactCurrencyRate_pipe as copy into FactCurrencyRate from @my_stage/FactCurrencyRate/ file_format = my_format;
create or replace pipe FactFinance_pipe as copy into FactFinance from @my_stage/FactFinance/ file_format = my_format;
create or replace pipe FactInternetSales_pipe as copy into FactInternetSales from @my_stage/FactInternetSales/ file_format = my_format;
create or replace pipe FactInternetSalesReason_pipe as copy into FactInternetSalesReason from @my_stage/FactInternetSalesReason/ file_format = my_format;
create or replace pipe FactProductInventory_pipe as copy into FactProductInventory from @my_stage/FactProductInventory/ file_format = my_format;
create or replace pipe FactResellerSales_pipe as copy into FactResellerSales from @my_stage/FactResellerSales/ file_format = my_format;
create or replace pipe FactSalesQuota_pipe as copy into FactSalesQuota from @my_stage/FactSalesQuota/ file_format = my_format;
create or replace pipe FactSurveyResponse_pipe as copy into FactSurveyResponse from @my_stage/FactSurveyResponse/ file_format = my_format;
create or replace pipe NewFactCurrencyRate_pipe as copy into NewFactCurrencyRate from @my_stage/NewFactCurrencyRate/ file_format = my_format;
create or replace pipe ProspectiveBuyer_pipe as copy into ProspectiveBuyer from @my_stage/ProspectiveBuyer/ file_format = my_format;